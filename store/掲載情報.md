# ストア掲載情報

2026-09-07 に API から登録。**App Store Connect 側が正。** ここは要点と原稿。

| | |
|---|---|
| ストア名 | 盤面タスク |
| サブタイトル | リマインダーを Apple Watch の文字盤に |
| カテゴリ | 第1: 仕事効率化 ／ 第2: ユーティリティ |
| 年齢制限 | 4+ |
| 価格 | 無料。App内課金は「コーヒー1杯」（消耗型・200円・`com.zzzjjj080.banmentask.coffee`）のみ |
| 配信地域 | 日本のみ（UIが日本語だけのため。広げるときは課金製品の地域も一緒に広げる） |
| サポート | https://zzzjjj080.github.io/banmentask/ |
| プライバシーポリシー | https://zzzjjj080.github.io/banmentask/privacy.html |
| 著作権 | 2026 Jin Nakamura |
| リリース | 手動 |
| App Privacy | **本人が画面で申告する（API に無い）。** 「データを収集しません」。課金は Apple が処理し、開発者は受け取らない |

## キーワード（100字まで。名前の語は入れない）

```
リマインダー,文字盤,コンプリケーション,Apple Watch,ウィジェット,ToDo,予定,カレンダー,やること,時計,常時表示,優先度
```

## プロモーションテキスト

腕を上げれば、次にやることが見える。純正リマインダーの上位2件を Apple Watch の文字盤に常時表示。

## 概要

盤面タスクは、純正リマインダーの上位2件を Apple Watch の文字盤に常時表示するアプリです。

腕を上げるだけで「いま何をやるか」が見えます。リマインダーのデータはそのまま。Siri での追加も、他の端末との同期も、純正アプリでの閲覧も、これまで通り使えます。

■ 文字盤に出る
・リマインダーの上位2件（最大4行）を文字盤のコンプリケーションに表示
・対応文字盤: モジュラー / モジュラーコンパクト / インフォグラフ モジュラー
・これからの予定（カレンダー）も一緒に出せます。行数と、そのうち何行を予定に使うかを選べます

■ iPhone で並べる
・ドラッグで並べ替えると、その順番がリマインダー本体の優先度として保存されます
・追加・完了・改名は盤面タスクの中でできます。その瞬間に文字盤へ送られます
・ホーム画面ウィジェット（横長・縦長・正方形）でも同じ一覧が見られ、○を押して完了できます

■ 自動で最新になる
・純正リマインダーを閉じた瞬間に文字盤を更新するオートメーション（ショートカット）を1回設定すれば、あとは何もしなくてよい
・文字盤をタップすると、その場で最新の内容になります

■ データの扱い
・データは iPhone のリマインダーとカレンダーにあるものだけ。アプリが独自に保存するものはありません
・アカウント不要。広告なし。解析なし
・App内課金は、開発者にコーヒーを奢る投げ銭だけ。機能は変わりません

Apple Watch とペアリングした iPhone でお使いください。

## 審査メモ（英語）

This app shows the top items of the built-in Reminders app as an Apple Watch face complication. It is designed for an iPhone paired with an Apple Watch.

On first launch the iPhone app asks for Reminders access (and, only if you enable calendar rows, Calendar access). Please allow it. If the test device has no reminders, add a few with the "＋" row at the bottom of the list; they are written to the built-in Reminders app.

To see the complication on the Watch: long-press the watch face → Edit → choose the rectangular slot → 盤面タスク. Supported faces: Modular, Modular Compact, Infograph Modular. Without a Watch, the iPhone home-screen widget shows the same list.

The only in-app purchase is a consumable tip ("コーヒー1杯" / one coffee) at the bottom of the iPhone screen. Nothing is unlocked or changed by purchasing; the app only shows a thank-you message.

No account, login, or credentials are required. The app makes no network requests other than StoreKit.

## 登録の状態（2026-09-13 1.1 を提出）

**1.1 (4) を審査に出した。承認されたらそのまま公開（AFTER_APPROVAL）。** 1.0 は公開中。

| 項目 | 状態 |
|---|---|
| バージョン 1.1 (4) | `WAITING_FOR_REVIEW`（ID `c2da795c-7057-40cd-8167-7379b208fc00`）|
| 提出枠 | `085fc147-6e37-4463-8d6f-4cd4e5ec80c0` |
| 更新内容 | アイコン刷新／時計まわりの画面の作り直し／名前つき15色／既定色を白に／色が保存されない不具合の修正 |
| iPhone の掲載画像 | 3枚とも新しい画面で撮り直し（02 は `BT_CONNECTED=1` で接続OKの表示）|
| Watch の掲載画像 | 1.0 のまま（Watch 側の画面は変わっていない）|
| 課金 | 1.0 で `APPROVED` 済み。今回は提出に入れていない |

**アイコンは本人判断で「ギリギリ」を攻めている。** 文字盤（曜日・日付・時刻・横長スロット）を角丸の縁で囲む図柄。
1.0 (2) は腕時計の絵で 5.2.5 却下（引き継ぎ書 4-115）。今回は竜頭・ボタン・バンドを外しただけなので、落ちうる。

**5.2.5 でまた却下されたら**

1. `store/icon-candidates/06-round-watch.png`（丸い腕時計）を `iOS/…/AppIcon.appiconset/icon.png` と `Watch/…/icon.png` に入れる
   （もっと安全にするなら `03-dial-list.png`）。描画は `tools/icon-concepts.swift`
2. ビルド番号を上げてアーカイブ → 書き出し設定は `tools/ExportOptions.plist`
3. 却下された提出枠はキャンセル（`canceled=true`）→ 新しい枠で出し直す（4-116）
4. 同じ理由で2回続いたことになるので、出し直す前に本人に方針だけ伝える（指示書の決まり）

アプリ ID `6809464254`、課金 ID `6809464583`。
